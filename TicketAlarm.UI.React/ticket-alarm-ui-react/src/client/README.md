# ticket_alarm

TicketAlarm - JavaScript client for ticket_alarm
No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: v1
- Package version: v1
- Generator version: 7.9.0-SNAPSHOT
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install ticket_alarm --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your ticket_alarm from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var TicketAlarm = require('ticket_alarm');

var defaultClient = TicketAlarm.ApiClient.instance;
// Configure API key authorization: Bearer
var Bearer = defaultClient.authentications['Bearer'];
Bearer.apiKey = "YOUR API KEY"
// Uncomment the following line to set a prefix for the API key, e.g. "Token" (defaults to null)
//Bearer.apiKeyPrefix['Authorization'] = "Token"

var api = new TicketAlarm.AlarmApi()
var idAlarm = 56; // {Number} 
var opts = {
  'alarmDto': new TicketAlarm.AlarmDto() // {AlarmDto} 
};
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.apiAlarmsIdAlarmPut(idAlarm, opts, callback);

```

## Documentation for API Endpoints

All URIs are relative to *http://localhost*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*TicketAlarm.AlarmApi* | [**apiAlarmsIdAlarmPut**](docs/AlarmApi.md#apiAlarmsIdAlarmPut) | **PUT** /api/alarms/{idAlarm} | 
*TicketAlarm.AlarmApi* | [**apiAlarmsIdShowGet**](docs/AlarmApi.md#apiAlarmsIdShowGet) | **GET** /api/alarms/{idShow} | 
*TicketAlarm.AlarmApi* | [**apiAlarmsPost**](docs/AlarmApi.md#apiAlarmsPost) | **POST** /api/alarms | 
*TicketAlarm.ArtistApi* | [**apiArtistsGet**](docs/ArtistApi.md#apiArtistsGet) | **GET** /api/artists | 
*TicketAlarm.ArtistApi* | [**apiArtistsPost**](docs/ArtistApi.md#apiArtistsPost) | **POST** /api/artists | 
*TicketAlarm.AvailabilityApi* | [**apiAvailabilitysPost**](docs/AvailabilityApi.md#apiAvailabilitysPost) | **POST** /api/availabilitys | 
*TicketAlarm.EmailApi* | [**apiEmailPost**](docs/EmailApi.md#apiEmailPost) | **POST** /api/email | 
*TicketAlarm.ShowApi* | [**apiShowsGet**](docs/ShowApi.md#apiShowsGet) | **GET** /api/shows | 
*TicketAlarm.ShowApi* | [**apiShowsIdShowGet**](docs/ShowApi.md#apiShowsIdShowGet) | **GET** /api/shows/{idShow} | 
*TicketAlarm.ShowApi* | [**apiShowsIdShowPut**](docs/ShowApi.md#apiShowsIdShowPut) | **PUT** /api/shows/{idShow} | 
*TicketAlarm.ShowApi* | [**apiShowsPost**](docs/ShowApi.md#apiShowsPost) | **POST** /api/shows | 
*TicketAlarm.UserApi* | [**authenticatePost**](docs/UserApi.md#authenticatePost) | **POST** /authenticate | 


## Documentation for Models

 - [TicketAlarm.AlarmDto](docs/AlarmDto.md)
 - [TicketAlarm.ArtistDto](docs/ArtistDto.md)
 - [TicketAlarm.AvailabilityDto](docs/AvailabilityDto.md)
 - [TicketAlarm.LoginUserDto](docs/LoginUserDto.md)
 - [TicketAlarm.ShowDto](docs/ShowDto.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### Bearer


- **Type**: API key
- **API key parameter name**: Authorization
- **Location**: HTTP header

